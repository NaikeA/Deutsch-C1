{
  "name": "deutsch-auf-c1-android",
  "version": "1.0.0",
  "private": true,
  "description": "Self-contained Android package for Deutsch auf C1",
  "scripts": {
    "android:init": "cap add android",
    "android:sync": "cap sync android",
    "android:open": "cap open android",
    "android:debug-apk": "npm run android:sync && cd android && ./gradlew assembleDebug",
    "android:release-apk": "npm run android:sync && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
